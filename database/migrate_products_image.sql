-- Photo for product listings (cover + gallery)
USE zakapeiku;

-- Cover (single)
-- ALTER TABLE products ADD COLUMN image VARCHAR(255) DEFAULT NULL AFTER location;

-- Gallery JSON (up to 3 filenames)
-- ALTER TABLE products ADD COLUMN images TEXT DEFAULT NULL AFTER image;
