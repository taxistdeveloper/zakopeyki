<?php

/**
 * Google OAuth 2.0 — скопируйте в config/google.php и заполните ключи.
 *
 * Authorized redirect URI в Google Cloud Console (ровно так):
 *   https://zakopeyki.kz/login
 */
return [
    'client_id' => getenv('GOOGLE_CLIENT_ID') ?: '',
    'client_secret' => getenv('GOOGLE_CLIENT_SECRET') ?: '',
    // Обязательно на проде — иначе за прокси уйдёт https://localhost/login
    'redirect_uri' => getenv('GOOGLE_REDIRECT_URI') ?: 'https://zakopeyki.kz/login',
    'allowed_hosts' => ['zakopeyki.kz', 'localhost', '127.0.0.1'],
];
